---
created: 2025-10-16  00:06
tags:
  - 爬虫
  - python
---
该日志包括2025.10.15之前的内容
#  2025-10-16 开发日志

## **完成了什么？**
### github
第一次把所有代码全部提交到github远程仓库中，仓库url见[[log：绘绘的视频数据分析]]
给所有json文件添加到ignore文件中
### 代码部分
使用bilibili api库结合ai写了一个爬取目标用户的视频数据代码
使用ai改正了原代码中无法正确爬取点赞投币收藏等动态数据问题
使用ai对json数据进行二次处理，把有用的字段单独拿出来整理到json文件中
对所有生成的json文件，确认有效后放置于JSON_SAVE文件夹中并改名防止被程序覆写
# **主要代码详解**：
## get_user_videos(uid)函数
该代码被更正过：[[1解决视频数据可能获取不全的问题]]
获取用户的投稿视频数据
```python
async def get_user_videos(uid):
    # 创建用户对象
    u = user.User(uid=uid)

    # 获取用户投稿视频列表的第一页，以获取视频总数和其他分页信息
    first_page_data = await u.get_videos(pn=1, ps=30)

    # 获取视频总数
    total_count = first_page_data.get('page', {}).get('count', 0)
    print(f"UP主共有 {total_count} 个视频")

    # 计算总页数（每页30条数据）
    pages = (total_count + 29) // 30  # 向上取整
    print(f"需要获取 {pages} 页数据")

    # 如果只有一页，直接返回第一页数据
    if pages <= 1:
        return first_page_data

    # 合并所有页面的数据
    all_videos = first_page_data.get('list', {}).get('vlist', []).copy()

    # 获取剩余页面的数据
    for page in range(2, pages + 1):
        print(f"正在获取第 {page}/{pages} 页数据...")
        page_data = await u.get_videos(pn=page, ps=30)
        page_videos = page_data.get('list', {}).get('vlist', [])
        all_videos.extend(page_videos)

        # 添加延迟以避免请求过于频繁
        await asyncio.sleep(0.5)

    # 构造完整的数据结构
    result = first_page_data.copy()
    result['list'] = result.get('list', {}).copy()
    result['list']['vlist'] = all_videos
    result['page'] = result.get('page', {}).copy()
    result['page']['count'] = len(all_videos)
    return result
```
- 使用bilibili_api库创建用户对象
- get_videos()函数获取第一页信息
- 注意！其中get的字段是通过对样例json文件进行分析确定可以通过这些字段确定视频总数
##  extract_analytics_data(videos_data)
从视频数据中提取用于分析的关键维度数据
该代码被优化过：[[2优化主爬虫extract_basic_data函数]]
```python
def extract_basic_data(videos_data):
    basic_data = []

    # 遍历视频列表
    video_list = videos_data.get('list', {}).get('vlist', [])
    for video in video_list:

        # 只提取BV号，用于后续详细数据获取
        data_point = {
            'title': video.get('title', ''),
            'bvid': video.get('bvid', ''),
            'typeid': video.get('typeid', 0),
            'created': video.get('created', 0),
            'length': video.get('length', ''),
            'play': video.get('play', 0),
            'comment': video.get('comment', 0)
        }

        basic_data.append(data_point)
    return basic_data
```
- 遍历使用bilibli api库提取出来的dict数据结构，从中提取出我要的内容（bv号）用于在detailed_video_stats文件中获取所有视频的准去信息
# next time内容
你需要更新完主函数内容和一个detailed_video_stats文件的代码介绍，并继续读代码检查是否有其他内容需要更新，本次日志和下次日志是给过去还债，为了确保日志习惯被良好保存，下次内容禁止添加到本日日志中！

---